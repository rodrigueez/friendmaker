# friendmaker 上游原项目与分支对比

> 上游：[zhouxiyu1997/friendmaker](https://github.com/zhouxiyu1997/friendmaker)  
> 分支：[rodrigueez/friendmaker](https://github.com/rodrigueez/friendmaker)（`codex-tomodachi-palette-preview-timer`）  
> 统计截止：2026-05-03，8 个提交领先上游，11 个文件变更，+3819 / -110 行

---

## 功能对比总览

| 功能 | 上游原项目 | 本分支 |
|------|:---:|:---:|
| 图片上传、缩放、偏移 | ✅ | ✅ |
| 三种基础色彩量化（单色/4色/全色） | ✅ | ✅ |
| 绘制时间估算 | ✅ | ✅ |
| 双遍绘制（粗笔 + 细笔修正） | ✅ | ✅ |
| 基础调色板指令预览 | ✅ | ✅ |
| 生成命令、导出 bc 文件 | ✅ | ✅ |
| **Tomodachi 官方色盘绘制计时** | ❌ | ✅ |
| **官方色盘像素数标注** | ❌ | ✅ |
| **自定义指令预览（第四标签页）** | ❌ | ✅ |
| **高级调色板控制： 抖动** | ❌ | ✅（Floyd-Steinberg / Atkinson / Bayer 4x4） |
| **高级调色板控制： 色彩距离** | ❌ | ✅（加权 RGB / 欧几里得 RGB / CIE Lab） |
| **高级调色板控制： 图像调整** | ❌ | ✅（亮度 / 对比度 / 饱和度） |
| **高级调色板控制： 合并同类色** | ❌ | ✅（可调阈值） |
| **绘制速度控制** | ❌ | ✅ |
| **命令生成算法优化： 垂直 L 命令** | ❌ | ✅ |
| **命令生成算法优化： 单次分组扫描** | ❌ | ✅ |
| **命令生成算法优化： 穷举最优路径** | ❌ | ✅ |

---

## 各模块详细对比

### 1. 前端 UI（`index.html` / `app.js` / `styles.css`）

| 上游 | 本分支 |
|------|--------|
| 3 个标签页（上传、调色板、设置） | **4 个标签页**，新增"自定义命令" |
| 基础设置项 | 基础设置 + **高级调色板控制**（抖动、色彩距离、亮度/对比度/饱和度滑块） |
| 无 | **合并同类色**复选框 + 阈值滑块 |
| 无 | **绘制速度控制**（hover 延时、笔画速度等） |
| 基础预览 | 基础预览 + **官方色盘像素数标注**（每个色块显示用到的像素数，含 1.2k 格式化） |
| app.js ~300 行 | app.js **+1134 行** |

### 2. 独立 Tomodachi 色盘页面（`tomodachi-palette.html`）

| 上游 | 本分支 |
|------|--------|
| **不存在** | 全新 1594 行独立页面 |

功能：
- 上传图片 → 直接量化到官方 84 色调色板
- 显示色盘预览、已用颜色列表
- 合并同类色（带阈值调节）
- 预估绘制耗时
- 生成 bc 命令文件

### 3. 服务端 API（`server.ts`）

| 上游 | 本分支 |
|------|--------|
| 接收基础生成参数 | 新增接收：抖动模式/量、色彩距离模式、亮度/对比度/饱和度、合并同类色 + 阈值 |
| 返回基础 DrawPlan | 返回新增：`colorCounts`、各 pass 预览 PNG、合并后的调色板信息 |

### 4. 命令生成管线

#### 4.1 垂直 L 命令优化（`scanline.ts`）

- **上游**：仅水平方向连续同色像素合并为 `L <dx> 0`，垂直方向逐个发 `M + P`
- **分支**：横竖双向均检测，连续垂直段发 `L 0 <dy>`，减少命令数 5-50%（视图像内容）

#### 4.2 单次分组扫描（`scanline.ts`）

- **上游**：`getPixelsByColor()` 对每种颜色全图扫描一遍（84 色 = 84 遍）
- **分支**：入口一次 `groupPixelsByColor()` 生成 `Map<colorIndex, Pixel[]>`，后续直接使用

#### 4.3 路径穷举优化（`scanline.ts`）

- **上游**：连通组件间始终用最近邻贪心
- **分支**：≤8 个组件时穷举所有排列找最优顺序，>8 个组件回退贪心

#### 4.4 合并同类色（`pixelize.ts`）

- **上游**：不存在
- **分支**：`mergeSimilarColorsInMap()` 在像素化后按频率优先合并视觉相近的调色板颜色，减少换色次数

### 5. 颜色计数（`pixelize.ts` / `generateDrawPlan.ts` / `types.ts`）

- **上游**：`PixelizationResult` 无 `colorCounts`
- **分支**：全链路传递每个颜色索引的像素数，前端在官方色盘上标注

---

## 技术统计

```
11 files changed, 3819 insertions(+), 110 deletions(-)

 apps/desktop/src/web/static/app.js                 | 1134 +-
 apps/desktop/src/web/static/index.html             |  261 +-
 apps/desktop/src/web/static/styles.css              |   59 +-
 apps/desktop/src/web/server.ts                      |   68 +-
 apps/desktop/src/app/generateDrawPlan.ts            |  114 +-
 apps/desktop/src/image/pixelize.ts                  |  115 +-
 apps/desktop/src/path/scanline.ts                   |  228 +-
 apps/desktop/src/web/static/tomodachi-palette.html  | 1594 ++ (新文件)
 apps/desktop/src/types.ts                           |    1 +
 package-lock.json                                   |   17 +-
```

---

## 兼容性

- 上游的所有 API 和功能**完全保留**，分支为纯增量修改
- 新增的高级选项均有合理默认值（关闭状态），不影响现有用户行为
- 生成命令格式与上游**完全兼容**（bc 协议不变），仅优化命令数量
- 分支基于上游 main 的 ff merge，无冲突
